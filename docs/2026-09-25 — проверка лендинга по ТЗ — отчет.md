# 2026-09-25 — проверка лендинга по ТЗ — отчет

**Вердикт первой проверки:** needs_edit (1 major, 4 minor) → major и значимые minor исправлены в тот же день, итог — **approved с оговорками** (см. «Не проверяемо автоматически»).

## Что проверялось

Агент landing-reviewer по `Документы/TZ_landing_markirovka.md` (v1.3): раздел 5, критерии приёмки, приложение А. Окружение: локальный сервер, curl, node --check. Lighthouse/Playwright в окружении недоступны — числовые метрики прогнать нельзя.

## Результаты проверки (после исправлений)

| Пункт | Результат |
|---|---|
| Hero-заголовок дословно «ТАМОЖЕННЫЙ СКЛАД В КАЗАНИ!» | PASS |
| Услуги: алкоголь первым + бейдж «Лицензия РАР», 10 категорий | PASS |
| FAQ: 12 вопросов-ответов посимвольно равны приложению А | PASS (программное сравнение, 0 расхождений) |
| 4 телефона + email посимвольно, все `tel:`-ссылки | PASS |
| Режим работы, крупно «Казань», точный адрес + Казанский таможенный пост | PASS |
| Встроенная Яндекс.Карта | PASS (см. замечание про координаты) |
| Каналы: email + MAX, без Telegram/WhatsApp | PASS |
| Форма: имя+телефон+согласие обязательны, понятные ошибки, honeypot + таймер | PASS |
| Валидация телефона: мобильные и городские RU-номера | PASS (после исправления — см. ниже) |
| Политика ПДн открывается, чекбокс обязателен | PASS |
| SEO: title/description/keywords (все 7 запросов ТЗ), lang=ru, og-теги, favicon | PASS |
| Локальные ресурсы: все src/href → 200 | PASS |
| JS-синтаксис (3 inline-скрипта, node --check) | PASS |
| Адаптив по коду (медиа-запросы 860/600, кнопки hero на всю ширину) | PASS |
| Плавающая кнопка (появление после hero) | PASS |

## Исправлено по итогам проверки

1. **Major — валидация отсекала городские номера** (принимались только мобильные 6/7/9). Теперь принимаются любые валидные RU-номера: 11 цифр, `+7/8` + код (3, 4, 6, 7, 8, 9), включая городские без кода страны (`843...`, `495...` → `+7...`).
2. Контраст текстовых ссылок: введён `--c-link #005eb8` (≥4.5:1 на белом); бейдж «Лицензия РАР» затемнён до #1f7a52.
3. Контраст плавающей кнопки: фон затемнён до #2d8a61 (белый текст ≥3:1).
4. Добавлен favicon (SVG data-URI, «ТС» на тёмно-синем).
5. Неиспользуемые фото (warehouse-hero-1..5, ≈1,2 МБ) удалены из репозитория — в публикации только используемые файлы.

## Осталось (minor, осознанно отложено)

- **Карта — текстовым поиском**, не координатами метки: виджет может показать не ту точку (адрес в Зеленодольском районе неоднозначен). Нужны координаты склада или визуальная проверка заказчиком.
- **og:image** — вписать абсолютный URL после выбора домена (этап 3).
- **Координация Lighthouse** — прогнать перед сдачей заказчику (инструмент отсутствовал в окружении).

## Не проверяемо автоматически (критерии ТЗ)

| № | Критерий | Статус |
|---|---|---|
| 1 | Первый экран на телефоне без прокрутки | по коду — ок; финально проверить на устройстве |
| 2 | Диалог звонка по `tel:` | код ок; проверить на устройстве |
| 3 | Доставка email + MAX за 1–2 мин | демо-mailto; реальная доставка — на хостинге (замена `submitLead()`) |
| 6 | Цель в Метрике | placeholder `XXXXXXXX` — вписать ID счётчика, цель `lead_form` уже в коде |
| 7 | Подсчёт заявок | инструкция в README.md |

## Публикация

Живой сайт: https://basilvs.github.io/Kazan_TC_Landing/ (ветка gh-pages). Лендинг/политика/фото — 200; КП и ТЗ по URL сайта недоступны (404).